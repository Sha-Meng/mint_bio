---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-03-22T13:55:38.808Z
provider: 
---

## OpenSpec 执行规则

本项目后续默认采用 **OpenSpec（spec-driven）** 范式执行任务，除非用户明确要求只做口头讨论或仅做一次性极小修改。

### 目标

- 先澄清需求，再设计方案，再实施改动，最后做验证与总结。
- 所有非琐碎任务都要留下可追踪的 spec/plan 记录，确保后续成员可以接手。
- 需求、方案、任务拆解、实现结果、验收结论必须保持一致。

### 默认工作流

#### 1. 需求澄清

开始执行前，先整理以下内容：

- 用户目标与业务背景
- 成功标准 / 验收标准
- 影响范围
- 已知约束与不做项

若信息不足，应先基于代码库与上下文自行补足可确认部分，再开展实现。

#### 2. 产出 spec/plan 文件

对 **非琐碎任务**，必须在 `.codebuddy/plans/` 下维护一个对应的 Markdown 计划文件，文件内容建议包含：

- `name`
- `overview`
- `todos`
- `User Requirements`
- `Product Overview`
- `Core Features`
- `Tech Stack Selection`
- `Implementation Approach`
- `Implementation Notes`
- `Architecture Design`
- `Directory Structure`
- `Key Code Structures`
- `Validation / Acceptance`

如已有同主题计划文件，优先复用并更新，不要重复创建多份语义相同的 spec。

#### 3. 先设计，后编码

在开始修改代码前，先明确：

- 为什么这样改
- 是否有备选方案，以及为什么不选
- 改动涉及哪些文件 / 模块
- 对现有行为、兼容性、数据结构、路由、配置的影响

禁止一上来直接改代码而没有方案依据。

#### 4. 用 todos 驱动实施

实施时必须按任务拆解推进：

- 每个 todo 应表达一个清晰、可验证的结果
- 同一时间只应有一个 `in_progress`
- 完成后立刻更新状态
- 若范围变化，应同步更新 spec 和 todos

#### 5. 验证与验收

完成实现后，至少补齐：

- 改动结果说明
- 关键验证步骤
- 如有可执行检查（lint / build / test / 手工验证），记录结论
- 未完成项、风险点、后续建议（如存在）

### 适用范围

以下情况默认走完整 OpenSpec 流程：

- 新功能开发
- 跨多个文件的修改
- 涉及架构、状态管理、路由、接口、构建配置、国际化、权限、部署的变更
- 需要分阶段推进或可被后续人员接手的任务

以下情况可走轻量流程，但仍应先说明目标与改动点：

- 单文件的小修复
- 文案微调
- 明确无歧义的局部样式修正

### 轻量流程要求

即使是轻量任务，也应至少做到：

- 说明目标
- 说明改动文件
- 说明验证方式
- 输出简短结果总结

### 文档与实现一致性

若实现过程中发现：

- 需求与原判断不一致
- 技术方案需要调整
- 范围扩大或缩小
- 存在外部依赖或阻塞

必须同步更新 `.codebuddy/plans/` 中的 spec 内容，不允许计划与实现长期脱节。

### 输出风格

对用户同步进展时，优先说明：

- 当前在哪个阶段
- 新发现了什么
- 接下来要做什么
- 最终产出了什么

### 项目约束优先级

执行 OpenSpec 流程时，仍需同时遵守项目内其他规则，尤其是：

- `.codebuddy/rules/` 下已有规则
- 国际化翻译必须严格依赖 `doc/zh-en/` 对照文件，禁止自行补译
- 用户明确指定的实现边界与限制