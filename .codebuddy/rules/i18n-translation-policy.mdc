---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-03-08T13:00:06.774Z
provider: 
---

## i18n 翻译规则

- **固定文案优先修改 Directus**：所有网站固定文案、页面标题、按钮、表单提示、首页模块小字等用户可见文案，默认都必须先修改 Directus `site_i18n_entries`，并同步递增 `site_i18n_settings.content_version` 触发前端运行时缓存刷新。
- **本地 JSON 仅作 fallback**：`src/i18n/*.json` 与 `src/i18n/modules/**/*.json` 只作为构建随包兜底内容。可以为了离线/接口异常兜底同步同 key 文案，但不能把只改本地 JSON 当作完成日常文案修改。
- **Directus key 覆盖桌面与移动端**：修改文案时必须确认桌面、移动端、聚合 key、拆分 key 是否分别读取不同 `key_path`，并一并更新对应 `site_i18n_entries`。
- **严格对照翻译文件**：所有翻译内容必须严格参照 `doc/zh-en/` 目录下的翻译对照文件（docx），只翻译对照文件中已有的字段。
- **禁止自行翻译**：对照文件中未覆盖的字段，**绝对不能**根据上下文语境自行翻译。未覆盖的字段应保留原始中文内容不变。
- **如有遗漏需确认**：如果发现对照文件中缺少某些字段的翻译，应告知用户并等待用户提供翻译，而不是自行补充。
